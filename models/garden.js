const mongoose = require("mongoose");
const Schema = mongoose.Schema;

// todo: outline data entries and potential
const gardenSchema = new Schema({

})

const Garden = mongoose.model("Garden", gardenSchema);

module.exports = Garden;
